name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Read version
      id: read_version
      run: |
        VERSION=$(cat ver.txt | tr -d '\n')
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

    - name: Build for Linux
      run: |
        cd plugins/yrdcdn
        go build -o yrdcdn-linux-amd64 .
        chmod +x yrdcdn-linux-amd64

    - name: Build for Windows
      run: |
        cd plugins/yrdcdn
        GOOS=windows GOARCH=amd64 go build -o yrdcdn-windows-amd64.exe .
        chmod +x yrdcdn-windows-amd64.exe

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: yrdcdn-builds
        path: |
          plugins/yrdcdn/yrdcdn-linux-amd64
          plugins/yrdcdn/yrdcdn-windows-amd64.exe

    - name: Create Release
      uses: ncipollo/release-action@v1
      with:
        tag: ${{ steps.read_version.outputs.VERSION }}
        name: Release ${{ steps.read_version.outputs.VERSION }}
        body: |
          Automated build release for yrdcdn plugin
          
          **Assets:**
          - yrdcdn-linux-amd64 - Linux x86_64
          - yrdcdn-windows-amd64.exe - Windows x86_64
        artifacts: |
          plugins/yrdcdn/yrdcdn-linux-amd64
          plugins/yrdcdn/yrdcdn-windows-amd64.exe
        skipIfReleaseExists: true
        token: ${{ secrets.GITHUB_TOKEN }}